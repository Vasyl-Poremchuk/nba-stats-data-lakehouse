Rules = [
    RowCount between 5292 and 21168,
    IsComplete "season_sk",
    ColumnLength "season_sk" = 32,
    IsComplete "team_sk",
    ColumnLength "team_sk" = 32,
    IsComplete "player_sk",
    ColumnLength "player_sk" = 32,
    IsComplete "player",
    IsComplete "uniform_number",
    IsComplete "position",
    ColumnValues "position" in ["PF","SG","C","SF","PG"] with threshold >= 0.99,
    IsComplete "height",
    IsComplete "weight",
    StandardDeviation "weight" between 22.4 and 30.3,
    ColumnValues "weight" between 118.8 and 358.6,
    IsComplete "birth_date",
    Completeness "country_of_birth" >= 0.9,
    IsComplete "years_experience",
    Completeness "college" >= 0.8,
    IsComplete "regular_season_awards",
    IsComplete "playoffs_awards",
    ColumnValues "playoffs_awards" in ["No Awards","Finals MVP-1"],
    ColumnValues "playoffs_awards" in ["No Awards"] with threshold >= 0.99,
]
